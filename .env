# Public site URL (no trailing slash). Used for canonical + link-preview tags
# in index.html — WhatsApp/Facebook need absolute image URLs. Replace with the
# real domain before deploying.
VITE_SITE_URL=https://example.com
